{% extends "front/search.html.twig" %}
{% block main %}
    
    <h2>la liste des catégories de la boutique</h2>
    {% for category in categorys %}
        <article>
         <h3><a href="{{path('front_show_category', {'id':category.id})}}">{{category.name}}</a></h3>
         <p>la description : {{category.description}}</p>
        </article>
        <hr>
    {% endfor %}
{% endblock %}